# Decisions log

Every modeling assumption and judgement call in the calculator: what it is, why, and where to change it.
**D-numbers** are referenced from code comments and from the in-app "How this works" page.
Items marked *(core)* are the central design choices described in [DESIGN.md](DESIGN.md); the rest are
implementation details. Dollar figures below describe the data as of the last entry in
"Data update history" at the end; the live values are in `src/data/rules.ts`.

## Model

| # | Decision | Why | Where to change |
|---|---|---|---|
| D1 | *(core)* Year-by-year cash-flow simulation; FIRE numbers are **solved**, not formulas. The 25× figure is shown only as a sanity check. | Social Security timing, two spouses, taxes, and backtesting all need a year-by-year engine. | `src/engine/simulate.ts` |
| D2 | *(core)* Success = never short of money before the **younger** spouse reaches the end age (default 96, Fidelity). Target success rate 90% (Fidelity "significantly below average"). | Conservative and matches Fidelity. | Assumptions panel |
| D3 | *(core)* Main method: **block bootstrap** of real history (10,000 paths). Cross-check: every complete **historical window**. Every success rate shown, and every FIRE result, uses the **stricter** (lower) of the two. | Bootstrap makes new but realistic paths; history is real but a small sample. | `src/engine/solve.ts` `combine` |
| D4 | Bootstrap block length **5 years**, circular (wraps from the last data year back to 1871), seeded RNG so identical inputs give identical answers. | Long enough to keep multi-year crashes and inflation spells together, short enough for variety. | Assumptions → chunk size |
| D5 | The solver's search steps use **2,000** of the paths; every answer is then re-checked on all 10,000 and moved later/higher until it passes there. | Keeps a full recalculation to seconds. | `src/engine/solve.ts` |
| D6 | Everything is in **today's (real) dollars**. Returns are converted using each historical year's actual inflation; amounts fixed in nominal dollars (cost basis, Roth principal) shrink with it (D63). | No inflation forecast needed; matches FI Calc/ProjectionLab "real" mode. | — |
| D7 | Money moves at the **start of each year** (contributions, withdrawals), then the year's return applies. | Same as FI Calc. Withdrawals-first is slightly conservative. | `src/engine/simulate.ts` |
| D8 | *(core)* One asset mix for all invested accounts, rebalanced yearly; default **70% stocks / 25% bonds / 5% cash** (Fidelity). Fund fees **0.10%/yr** subtracted from returns. | Fidelity's FI Planner mix. | Assumptions panel |
| D9 | The **cash account** (emergency fund) earns the T-bill return, not the portfolio mix. | It is cash. | `src/engine/simulate.ts` |

## Data

| # | Decision | Why |
|---|---|---|
| D10 | Stocks, 10-year Treasury bonds and CPI: Shiller `ie_data.xls`, **January-to-January** years from 1871. | Same source as FI Calc/cFIREsim. |
| D11 | *(core)* Cash 1928+: Damodaran 3-month T-bill (average rate over the calendar year). Before 1928: the January 10-year yield as income only (no price change). | No free T-bill series before 1928; yield curves were fairly flat then. |
| D12 | Raw spreadsheets are **not committed**; `npm run data:build` re-downloads them. The generated `src/data/market.json` **is** committed. | Small repo, still reproducible. Shiller's download URL changes on each update — see the script header. |

## People, work and spending

| # | Decision | Why |
|---|---|---|
| D13 | *(core)* Two-person household. Both spouses stop working in the same calendar year (the "household retirement date"); both are assumed alive through the whole plan. | Keeps the model and results simple; separate retirement dates and survivor modeling are possible v2 features. |
| D14 | Ages are computed by calendar year (`year − birth year`). 59½ access starts the **calendar year a person turns 60**; Medicare (65) starts the calendar year they turn 65. | Annual model; 59½ rounded the conservative way. |
| D15 | Salaries and contributions grow **1.5%/yr above inflation** (Fidelity) until work stops, but 401(k)+IRA and HSA contributions are **capped every year at the IRS limits**, which stay flat in today's dollars (catch-ups from 50 / 55). Money above a limit is not saved anywhere else. Employer match is not capped. | Limits only keep pace with inflation; letting contributions outgrow them would be optimistic. |
| D16 | While working, the paycheck covers all spending; the portfolio only receives contributions (plus D17 items not in today's budget, and D49). | Simplest faithful model of "still working". |
| D17 | **Dated items before retirement.** An *ongoing* item already running at the plan start (a mortgage, rent received) is part of today's budget: the paycheck covers it until retirement, and it counts from the retirement date on. Every other item dated before retirement (a roof, a car, a new ongoing cost, a home sale) moves money that year: costs come from cash, then the brokerage account (paying federal + state tax on the realized gain, stacked on wages); inflows are saved to the brokerage account at full basis, after income tax on taxed income (D66). A cost that cash and brokerage can't cover **fails the path**. | Costs not in today's budget are real and must be paid from savings; for Coast FIRE they span every year until the coast age. Items already paid today would otherwise be counted twice (the Traditional/Chubby defaults subtract them, D18). Failing when savings can't pay is consistent with "never run out"; with a meaningful brokerage balance it rarely triggers. Also keeps success non-decreasing in the retirement year (a pre-retirement home sale no longer vanishes if you retire later). |
| D18 | Traditional spending must **exclude** healthcare and dated items (added separately). The Fidelity default is **0.85 × (current spending − ongoing dated expenses already being paid today)**. Healthcare paid out of pocket today must be subtracted by hand (the hint says so). | Otherwise a mortgage listed as a dated item would be counted twice. |
| D19 | "Fixed dollars" dated items (e.g. mortgage P&I) shrink by **each path's own inflation** since the plan start. | Exact treatment of nominal payments. |
| D20 | Money coming in (dated income items, surplus Social Security) first covers that year's spending (taxed income also adds to that year's tax, D66); any surplus goes into the taxable account with full cost basis. | Home-sale proceeds etc. |
| D57 | Chubby spending defaults to **1.2 × (current spending − ongoing dated expenses already being paid today)** — the D18 base — via a "Use default" button. Clearing it skips the Chubby result. | Online definitions of Chubby FIRE are dollar bands (~$80k–$200k/yr), not a ratio. As a lifestyle it is comfortable, not frugal, but short of Fat FIRE's no-budget luxury (roughly 1.5×+ today's spending). 1.0× landed close to Traditional (0.85×): in the example plan only 1–2 years later and ~12% more money. 1.2× is a clear step up (3–4 years later, ~25% more) while staying well below Fat. Same base as D18 so a mortgage isn't counted twice. Change `CHUBBY_SPENDING_FACTOR` in `src/engine/defaults.ts`. |

## Healthcare

| # | Decision | Why |
|---|---|---|
| D21 | *(core)* Per person: pre-65 cost applies only in years the household is **not working**; Medicare-phase cost from 65 on. Healthcare grows **1.5%/yr above inflation from today**. No ACA subsidies (full price). | Conservative; subsidies need yearly-income (MAGI) modeling (v2). |
| D22 | HSA balances pay healthcare first (tax-free). HSA is also the very last resort for other spending (ordinary tax, +20% penalty — see D41). | Standard HSA rules. |
| D58 | Example healthcare costs per person: **$16,000/yr before 65** and **$7,500/yr from 65** (today's dollars). The "?" help lists the 2026 reference prices. Pre-65 is one flat number, not age-rated. | Pre-65: 2026 US average benchmark silver premium is $7,500/yr at 40 (KFF, $625/mo) and ~$15,900 at 60; under the federal age curve (64 pays 3× a 21-year-old) the average from a mid-50s retirement to 65 is ~$14k, plus ~$2–3k out-of-pocket (2026 limit $10,600). Unsubsidized premiums rose 26% for 2026 as the enhanced tax credits expired. From 65: Part B $2,435 + Medigap G ~$2,650 + Part D ~$400 + ~$1,500–2,000 out-of-pocket, dental and vision. State prices vary widely; IRMAA isn't modeled. Sources: KFF 2026 benchmark premiums and older-adults brief; CMS 2026 Medicare costs. Refresh with the yearly data update. |

## Social Security

| # | Decision | Why |
|---|---|---|
| D23 | *(core)* Benefit computed from a pasted SSA earnings record (taxed Social Security earnings), with the statement's full-retirement-age benefit as a fallback. | SSA statements assume you keep working until you claim, which overstates benefits for early retirees. |
| D24 | Past earnings indexed to the latest **average wage index** year; later earnings (past and projected) at face value in today's dollars; the matching **bend points** (first eligibility = AWI year + 2); future earnings capped at the **taxable maximum**. Result is in today's dollars. | SSA's own "today's dollars" method. By default ignores real growth of the national wage index between now and 60, which *understates* benefits (conservative): at 0.9%/yr (1985–2024) by about 22% for someone now 40 and 11% at 50. D77 makes the growth an editable setting. |
| D25 | Claim ages are whole years 62–70. First benefit year counts the months actually received that year: entitlement starts in the birthday month (the month after it at 62), and each month's benefit arrives the following month, so the first year pays 12 − birth month (one fewer at 62), and later years 12. Early/late adjustments and spousal benefit (50% of the other's PIA, reduced if started early, no delayed credits) follow SSA rules; spousal benefit starts only once the other spouse has claimed. No earnings test. | SSA rules; the earnings test mostly defers rather than loses benefits. The model is a yearly cash flow, so the first year counts cash received (v1 counted one to two months more, overpaying $2–5k once). |
| D26 | *(core)* Trust-fund cut: 100% before **2032**, then a straight line from **78% (2032)** to **62% (2100)**, flat after (2026 Trustees Report, OASI). Editable, including 100% (no cut) or 0% (no Social Security). | Plans conservatively for the officially projected shortfall. |

## Accounts, withdrawals and taxes

| # | Decision | Why |
|---|---|---|
| D27 | *(core)* Before 59½: cash → taxable → Roth contributions and conversions ≥5 years old → pre-tax with 10% penalty (flagged, not failure, D68) → unseasoned conversions (10% penalty) → Roth earnings (tax + penalty) → HSA. | Keeps penalties as a last resort. |
| D28 | After 59½: RMDs and the bracket fill first, then cash → taxable → more pre-tax → Roth → HSA. In mixed-age households each person follows their own access rules, pre-tax before Roth across the household. | Tax-efficient order: Roth keeps growing tax-free and pre-tax shrinks before RMDs. |
| D29 | *(core)* **Bracket fill** (Roth ladder): every retired year, pre-tax money is withdrawn up to the top of the chosen bracket (default **10%**). What spending doesn't need is converted to Roth. Not done in years the household still works. The year-by-year table shows the Roth money available without tax or penalty each year. | Converting builds early access to pre-tax money only if a conversion seasons (5 years) before its owner's 59½ (the calendar year they turn 60, D14), and pre-tax money comes from the older spouse first (D30); after 59½ the same rule shrinks future RMDs. Pending-work decision 3 moved the default from 12% to 10%: on the example plan at its 2039 date, 10% needed $2.622M with a 19.0% penalty rate and $365k left in a bad market, vs $2.695M / 37.5% / $74k at 12% and $2.631M / 15.7% / $291k with no conversions. The app doesn't yet count some benefits of converting (survivor single-filer taxes, Roth worth more to heirs), so the default is to be revisited once the survivor test exists. Plans saved with the old default keep 12% ("How this works" marks it as yours). |
| D30 | Among spouses, pre-tax withdrawals and fill come from the **older** spouse first. | Their account reaches 59½ and RMD age first. |
| D31 | RMDs from age **75** (born 1960+) or **73** (born 1951–59) using the IRS Uniform Lifetime Table — also in years still working (D49). | SECURE 2.0. |
| D32 | Federal tax: current-year MFJ brackets, standard deduction + extra per spouse 65+, LTCG 0/15/20% stacked on ordinary income, Social Security taxation (provisional income), 3.8% NIIT. Indexed amounts stay constant in real terms; **non-indexed** thresholds ($32k/$44k Social Security, $250k NIIT) shrink with the price level. The temporary 2025–2028 senior bonus deduction is ignored. | Conservative, current law. |
| D33 | State tax: one flat rate (default **5%**) on taxable income excluding Social Security. | State rules vary widely; a single editable rate covers any state, including 0% for no-income-tax states. |
| D34 | Brokerage dividends and interest, and cash-account interest, are **taxed every year** (D70); the rest of the growth is taxed as a gain when sold. | v1 left them untaxed on the grounds that most dividends fall in the 0% gains band. That didn't hold: the bracket fill and wages push investment income out of the band, and interest never qualifies for it. |

## App

| # | Decision | Why |
|---|---|---|
| D35 | *(core)* Local-only React + TypeScript + Vite app; the engine is UI-free and runs in a pool of three Web Workers. | Private by design; the page stays responsive during long calculations. |
| D36 | *(core)* Sessions are JSON files in a folder the user picks (File System Access API — Chrome/Edge). Firefox/Safari fall back to download/upload of the same JSON. | Survives browser resets; easy to back up. |
| D37 | Layout uses **all available width**: inputs in a scrollable left panel, results fill the rest, charts resize with the window. The "How this works" page is one column capped at 1100px so its intro and table line up. Stacks into one column below 1000px. | Charts need room. |
| D38 | The working plan is also auto-saved in the browser (localStorage) so a reload never loses typing. Sessions remain the real store. | Convenience; silently does nothing in private windows. |

## Implementation details

| # | Decision | Why | Where to change |
|---|---|---|---|
| D39 | Bracket-fill room ignores the year's capital gains, so filling the 12% bracket or higher can push gains from taxable sales out of the 0% LTCG band (visible in the tax table). | Real tradeoff between conversions and 0% gain harvesting; simple and visible. The 10% default leaves most of the 0% band free. | `src/engine/simulate.ts` |
| D40 | Capital losses are ignored (a sale never realizes a negative gain). | Rare for buy-and-hold; small effect. | `src/engine/simulate.ts` |
| D41 | HSA is one household pool. Non-medical HSA withdrawals (very last resort) pay the 20% penalty while the younger spouse is under 65. | Simpler; conservative. | `src/engine/context.ts` |
| D42 | The FIRE number at a future date uses the account mix from a projection at long-run average real returns (geometric means of the whole data set), scaled to the target total. | The target must be one number, but taxes depend on which accounts hold the money. | `src/engine/solve.ts` `projectState` |
| D43 | Earliest Traditional/Chubby date is searched up to the **younger** spouse's age 75 (never past the year before the plan ends, and at least the current year); beyond that the card says "Not reachable" and names the last year tried with both ages. | Past 75, early retirement is moot. Using the younger spouse makes the answer the same whoever is entered as "You"; with the first person's age, a 74-year-old married to a 58-year-old got "Not reachable" while swapping them gave 2031. | `src/engine/solve.ts` |
| D44 | The 25× sanity check uses spending + the first retired year's healthcare. Shown in the detail view for Traditional and Chubby (D69). | Healthcare is a separate line (D18). | `src/engine/solve.ts` |
| D45 | Portfolio bands chart shows the full scale by default, with a "zoom in on the weaker markets" toggle. | Honest default; the typical path can dwarf the bad-market lines. | `src/ui/Results.tsx` |
| D46 | Earnings import accepts pasted "year amount" rows or SSA's XML statement download (`FicaEarnings`). Multi-year range rows ("1981-1990") are skipped. The XML format follows the published my Social Security statement schema; if a download differs, paste the table rows instead. | Not verified against a real statement download. | `src/engine/earnings.ts` |
| D47 | **Two kinds of historical check.** Whole-plan results (earliest date, success today, the detail view, worst-years table) replay the plan from the plan start: "start year 1966" means the plan's first year behaves like 1966. The FIRE *number* is a retirement-only test, so its history leg uses windows that start at the retirement year. Worst-years table: failures first, earliest failure first, then lowest ending balance; a "Retiring into" column gives the market year the first retired year gets (start year + years until retirement), since "1953" for a 2039 retirement means retiring into 1966. | Each check matches the question it answers (D54). In the whole-plan replay each retirement start is reached through the years that preceded it in history — a boom before 1929 or 1966, but also bad saving stretches (1929–32, the 1970s). Checked on the example plan (2026 review), the whole-plan replay was *stricter* than a retirement-only replay from an average-growth balance (e.g. Traditional at 2038: 84.8% vs 98.2%), and the simulated markets were the binding check at the answer, so the whole-plan replay was kept for the date. | `src/engine/solve.ts` `histFor` |
| D48 | Example numbers on first launch are placeholders (two people aged 42/40, $100k salaries). Spending and healthcare placeholders follow D18, D57 and D58. | Every number is meant to be overwritten. | `src/engine/defaults.ts` |
| D49 | Social Security received and RMDs due **while the household still works** are saved to the taxable account after the extra federal + state tax they cause on top of wages (salaries minus pre-tax contributions). | An older spouse may claim or reach RMD age before the household retires; ignoring that income would understate savings. | `src/engine/simulate.ts` |
| D50 | **Coast number's account mix.** Coast "needed today" keeps today's balances and counts any money needed beyond them as if saved the way the household contributes today (401(k)/IRA incl. employer match, Roth, HSA, brokerage at full basis; cash savings left out); below today's total, today's balances are scaled down. With no contributions either, the extra goes to the brokerage account at full basis. The card's "?" help shows the mix. The Traditional/Chubby number still scales the projected mix (D42). | Scaling today's mix priced the whole number in whatever a tiny balance held: $1 in cash gave $3.02M against $676k for no savings at all, and a $20k emergency fund alone did the same. Topping up in the contribution mix is continuous at today's total and uses accounts the household actually saves in. | `scaleState` in `src/engine/simulate.ts`, `contributionMix` in `src/engine/solve.ts` |
| D51 | Retirement-only runs (the FIRE number) start both bootstrap and history at the same price level: average historical inflation compounded to the retirement year, matching the average-return projection of balances (D42). Whole-plan runs use each path's own inflation. | Both legs of "stricter of two" must describe the same scenario. | `src/engine/solve.ts` `successRate` |
| D52 | Before 59½, if the year's spending needs pre-tax money, the planned bracket-fill conversion is given up first (spending beats converting). | Otherwise the ladder could convert money the year needed and fail a path. | `src/engine/simulate.ts` `planDraws` |
| D53 | Unseasoned conversion principal withdrawn before 59½ pays only the 10% penalty (it was taxed on conversion); only Roth *earnings* are taxed again. Withdrawn conversions leave the 5-year history oldest-first. | IRS ordering rules. | `src/engine/simulate.ts` |
| D54 | **What the Traditional/Chubby card means.** *Earliest year*: first year the whole plan, simulated from today, reaches the target — a probability seen from today, so it holds only if the savings are really there then (the warnings panel says so, D67). *Savings needed when you retire*: the portfolio the retirement-only simulation needs on the day work stops. *Expected by then*: the typical and bad-market (10th percentile) balance the whole-plan simulation expects at that date. The three together explain each other. | Two numbers from different tests would otherwise look contradictory. | `src/engine/solve.ts`, `src/ui/Results.tsx` |
| D55 | Opening a plan whose start year is earlier than the current year shows a banner with a one-click "start plan in <this year>". | The yearly checkup must move the plan start, or ages and 59½/RMD timing are a year off. | `src/App.tsx` |
| D56 | **Plain-language UI.** On screen: "simulated markets" = block bootstrap paths; "real past markets" = historical windows; "the lower one counts" = stricter-of-two; "typical / below average / bad market (1 in 10)" = 50th / 25th / 10th percentile (Fidelity's average / below / significantly below average); "chance your money lasts" = success rate. Every input has "?" help (`src/ui/helpText.ts`) written for a non-expert. | Finance jargon made the results hard to read. | `src/ui/helpText.ts`, `src/ui/Results.tsx` |
| D59 | Saved results are flagged "recalculate" when the market data, tax/Social Security rules, wage-index year, Trustees Report **or the engine version** differ from the app's (`DATA_VERSIONS`). Bump `DATA_VERSIONS.engine` whenever a code change alters results for the same inputs. | Old results must never look current after a data refresh or an engine fix. | `src/engine/assumptions.ts` |
| D60 | Input guards: a plan longer than 120 years is rejected with a message (a typo such as plan start "202"); simulated markets are limited to 500–50,000. | Otherwise one typo runs for hours or runs the browser out of memory. | `src/engine/context.ts` `planYears`, `src/ui/InputsPanel.tsx` |
| D61 | **Local addresses and launcher.** Dev server on port **5391**, built copy and launcher on **4391** (fixed, strict), not Vite's default 5173/4173. The launcher serves only the file types the app uses (html, js, css, images, fonts) and 404s everything else, including `.json`. | The browser keeps the unsaved draft and the sessions-folder permission per `localhost:port`; any other project served on a shared default port could read them. A sessions folder kept inside the launcher's folder must not be downloadable by other local programs or accounts. | `vite.config.ts`, `public/serve.ps1` |
| D62 | Results opened from a session file are labelled as saved results with a **Recalculate** button, and damaged session files are rejected when opened (D71). | A session file can be edited or come from someone else; its results must not pass for the app's own calculation. | `src/App.tsx`, `src/ui/sessions.ts` |
| D63 | **Nominal amounts are deflated every year.** Taxable cost basis, Roth principal (contributions and conversions) and each conversion's 5-year seasoning amount are fixed in nominal dollars, so after each year's return they are divided by that year's inflation to stay in today's dollars. The FIRE number's average-return projection (D42) uses average inflation for the same reason. | Without it, capital gains were understated (a $100k position held 20 years at 5% real and 3% inflation has a 79% gain share, not 62%) and early Roth access was overstated. | `src/engine/simulate.ts` growth step, `src/engine/solve.ts` `projectState` |
| D64 | **Example numbers still in use.** A warning banner above the results names every input section (People, Balances, Yearly contributions, Spending, Healthcare, Social Security) where either person's part, or the household's part, still equals the example plan. People compares birth year and salary; Social Security compares the mode, statement benefit and whether earnings were imported. Assumptions and dated items are not checked: their defaults are deliberate, and the example has no dated items. The sections stay collapsed. | Example values slipped into results unnoticed: a couple who edited only People was told "You can stop saving now" on the example's $880k. A banner that stays until each section is edited is harder to miss than opening every section. "How this works" marks claim ages as default only when both are still the example's 67. | `untouchedSections` in `src/engine/defaults.ts`, banner in `src/App.tsx` |
| D65 | **Older plan files.** Session files and the browser draft keep `schemaVersion: 1`; fields added to the plan since are filled in when a plan is loaded (`migratePlan`). A missing assumption takes its default; a dated income item without the "taxed" flag is taxed (D66). Other missing or malformed fields are not guessed. | Old files must keep loading, and an untouched new setting must behave like before or take the cautious choice (decision 2 of the pending work). | `src/engine/migrate.ts` |
| D66 | **Dated income is taxed.** Each dated income item has a "Taxed as income" flag, on by default (and for items saved before it existed). Taxed income counts as ordinary income for federal and state tax and for Social Security taxation, and it uses up bracket-fill room, so the Roth fill converts less. While working it is stacked on wages (and on any D49 income) and saved after the extra tax it causes. Untaxed items (a home sale, a cash gift) move money without tax, as before. | v1 never taxed dated income, and told users to enter it after tax, which they couldn't do because the right rate is the engine's own marginal rate after the fill (a $40k pension was under-taxed by ~$10,800/yr). Pending-work decision 1: an optimistic error is fixed outright. Decision 2: an untouched setting takes the cautious choice, so the flag defaults to taxed. A home sale's gain above the $500k exclusion is not modeled; enter only the taxable part as a separate taxed item. | `src/engine/context.ts` (`realInTaxed`), `src/engine/simulate.ts`, `src/ui/DatedItemsEditor.tsx` |
| D67 | **Result cards stay crisp; cautions go in one panel below them.** The cards show target numbers only. Every caution about them is one short line in a "Before you act on these numbers" panel directly below the cards and above "Try a different retirement year", showing only the lines that apply to the current results and naming the FIRE type each is about. Longer explanations go in the detail view. First line: Traditional/Chubby dates assume you'll have "Savings needed" by then; re-run each year with real balances. | Pending-work decision 5: the target numbers must be easy to read, and warnings right below them are still seen before anyone acts; the bottom of the page would be missed. The old card sentence "Your date already allows for bad markets while you save" read as permission to retire on that date whatever the balance: arriving with the bad-market balance instead of the amount needed passed in only ~63% of markets. | `src/ui/warnings.ts`, `BeforeYouAct` in `src/ui/Results.tsx` |
| D68 | **Penalty paths count as successes, and the panel says how often.** A market that survives only by taking 401(k)/IRA or Roth money before 59½ with the 10% penalty still counts as a success. When more than **5%** of simulated markets pay the penalty at a FIRE type's earliest date, the warnings panel says "In X% of markets the {type} plan pays a 10% penalty on early 401(k)/IRA withdrawals". | Pending-work decision 4: the simulation already charges the penalty against the balances, so counting those markets as failures too would punish the same cost twice; what was missing was visibility (37.5% at the example's v1 date, shown only in the detail view). Below 5% the line would be noise. | `PENALTY_WARN` in `src/ui/warnings.ts`; `penaltyRate` in `solveTier` (`src/engine/solve.ts`) |
| D69 | **Coast caveat and the 4% rule check.** The warnings panel says Coast assumes both keep working until the coast age with pay covering all spending, and (when there are employer matches) that stopping saving gives up the matches. The "4% rule check" (D44) is no longer on any card: it is a small stat, "Rule-of-thumb check (4% rule)", in the detail view's savings panel for Traditional and Chubby only, with its caveats in the "?" help. | "You can stop saving now" depends on both jobs covering everything, and the model zeroes the match with every other contribution. The 4% figure is a retirement-age number that read as a contradiction next to Coast's "Needed today"; it is a reference, not a warning, so it goes in the detail view rather than the panel (pending-work decision 5). | `src/ui/warnings.ts`, `DetailView` in `src/ui/Results.tsx` |
| D70 | **Yearly investment income.** Each year the brokerage account pays dividends of **2%** on its stock share (qualified: taxed like long-term gains) and interest of **4%** on its bond share (ordinary income); its cash share and the cash account pay that market's own nominal T-bill rate (ordinary income). The income is earned on what stays invested after the year's withdrawals, counts toward Social Security taxation and the 3.8% NIIT, and is reinvested, so it adds to cost basis. **While working** the tax is figured on top of wages and everything else that year, and each account pays the tax on its own income (the rest is reinvested). **Retired**, it is part of that year's taxable income and the tax is paid from withdrawals like any other tax; the bracket fill leaves room for the interest (estimated on the start-of-year balances). | Fixed yields are simple and cautious: the S&P 500 yielded about 1.2–1.5% in 2025–26 and the 10-year Treasury about 4–4.5%; 2% leaves room for a higher-yield mix. Paying the working-years tax from the accounts is a judgement call: the paycheck already covers all spending (D16) and contributions are fixed, so the tax has nowhere else to come from; letting the paycheck pay it invisibly would have made results rosier than v1. Treasury interest is exempt from state tax, which the flat state rate ignores (slightly conservative). On the example plan the change mostly lowers savings while working (Coast's number rose ~3%); in retirement most of the income falls in low brackets and the added basis offsets it. | `TAXABLE_YIELDS` in `src/engine/context.ts`; `investmentIncome` in `src/engine/simulate.ts` |
| D71 | **Checking loaded plans.** One set of checks (`fieldProblems`) covers every field's type and its own limits: numbers must be numbers, whole where they should be, money never negative, rates and trust-fund shares 0–100%, claim ages 62–70, birth months 1–12, 50,000 simulated markets at most, and so on. A session file that fails is rejected with the first few problems (in the folder list it shows as "can't be opened" with the reason); a browser draft that fails is set aside under a second storage key and the example plan is shown with the reason. Checks that involve several fields and can go stale without any edit (the Coast age must be above today's age and before the plan ends) block **Calculate** with a message instead of blocking the load. A React error boundary offers to reset the unsaved plan if anything still crashes. | A damaged draft blanked the page on every load, and a balance saved as text moved Traditional from 2040 to 2027. Pending-work decision 6: impossible values are blocked, in files as well as on screen. Moving the plan start in a yearly checkup can put the Coast age in the past, and that must not make a session unopenable. | `src/engine/validate.ts`, `parseSession` in `src/ui/sessions.ts`, `loadDraft` in `src/App.tsx`, `src/ui/ErrorBoundary.tsx` |
| D72 | **Whole-number fields round.** Years, ages, months, chunk size, market count and seed round what is typed (96.5 → 97); a field shows the value actually kept once you leave it. The engine also refuses a fractional plan start, birth year or plan-to age with a readable message. | A fractional year count crashed Calculate, gave NaN or silently added $0 (a one-time item in 2030.5). Rounding matches what the field's step already suggested. | `parseFieldText` in `src/ui/format.ts`, `planYears` in `src/engine/context.ts` |
| D73 | **Representative markets in the detail view.** The year-by-year table ("typical market") and the account chart ("bad market, 1 in 10") each follow the one simulated market whose total savings stay closest to the 50th / 10th percentile line over the first 10 retired years (smallest sum of squared log differences). | They used to be picked by ending balance, and a market that ends typical can be far from typical early on, when the order of returns matters most: in the example the table's first row showed $4.12M against a typical line of $3.31M. | `detailFor` in `src/engine/solve.ts` |
| D74 | **What happens in the other 10%.** Next to the savings chart, the detail view says in what share of simulated markets savings run out, the median year they do (as a retirement year), and the household's Social Security and spending in that year (fixed-dollar items at average inflation). Not on the cards and not in the warnings panel. | A 90% badge alone reads as a guarantee or as a 1-in-10 chance of going broke soon after retiring; in the example, failures come in retirement year 29 or later, leaving about $40k/yr of Social Security against about $110k of spending. Pending-work decision 5 puts this longer explanation in the detail view, beside the chart of those markets. | `failures` in `detailFor` (`src/engine/solve.ts`), `failureText` in `src/ui/Results.tsx` |
| D75 | **Limitations in the app.** "How this works" ends with a "What this doesn't model" group: both spouses alive throughout and one retirement year (D13), Rule of 55 / 72(t), state tax details, IRMAA, flexible spending, where each investment is held, US-only markets and lower-than-history returns, and the other planned views. The warnings panel always ends with "All amounts are in today's dollars. These are estimates, not financial advice" and a link to that group (today's dollars were otherwise only in hover help, and are said once here rather than on every card figure). | The UI had no not-advice disclaimer, and these limits were documented only for developers. ACA subsidies stay in the Healthcare group. | `describeAssumptions` (`LIMITS_GROUP`) in `src/engine/assumptions.ts`, `src/ui/warnings.ts` |
| D76 | **Block the impossible, warn on the unusual.** Fields refuse, with a message saying why, values that can't be right: negative balances, contributions or spending (the message says to enter a debt as a dated expense), negative salary, healthcare or benefit, fees, tax rates and trust-fund shares outside 0–100%, raises and healthcare growth outside ±50%/yr, a Coast age at or below today's age or at or past the plan end, birth years more than 120 years back or in the future, and a retirement year past the year before the plan ends. The last accepted value is kept and the text reverts on leaving the field. Unusual but possible values are accepted with a note: fees above 3%, state tax above 15%, healthcare growth above 10% or below 0%. The same limits reject loaded files (D71). | Pending-work decision 6: blocking stops the math from producing meaningless answers (coast age 200 gave "stop saving now" with $900 needed; a 60% state tax left the tax loop unconverged); warnings keep real edge cases possible. The ±50% growth bound is a judgement call to keep typos out while allowing any realistic assumption. | `src/ui/InputsPanel.tsx` (limits at the top), `fieldBlock` in `src/ui/format.ts`, `src/engine/validate.ts` |
| D77 | **Social Security wage growth setting.** "Social Security wage growth above inflation" (Assumptions, advanced; default **0%**, limits ±5%) is the real growth of the national wage index after the latest published year. The benefit is multiplied by (1 + g)^(birth year + 60 − wage-index year); from an earnings record, pay after the wage-index year first counts against the higher wage level (divided by the same growth, with the taxable maximum rising with it), so the rise is a little smaller than for a statement estimate, which gets the multiplier alone. At 0% the results are exactly v1's. | Pending-work decision 1: fix the pessimistic error too, but keep today's behavior as the default so the caution is visible and changeable rather than hidden. Decision 2: an economic assumption lives in Assumptions (advanced) and an untouched setting behaves like before. The help and How-this-works state the effect (+27% at 40 and +14% at 50 for a statement benefit at the Trustees' 1.1%). Deflating future pay against national wages is the correct pairing with the multiplier; applying the multiplier alone would overstate record-based benefits. | `computePia`/`wageLevelAt60` in `src/engine/socialSecurity.ts`; the setting in `src/ui/InputsPanel.tsx` |
| D78 | **Borderline dates.** When a FIRE type's earliest date passes by less than **1.5 points** (e.g. 90.9% against a 90% target), the warnings panel says it is borderline and could be a year later. | The earliest year can move with the random seed when success sits just over the target (the v1 example's Traditional date was 2039 for seeds 1–3 and 2040 for seed 4, at 90.2%). 1.5 points is about the seed-to-seed spread measured there. | `BORDERLINE` in `src/ui/warnings.ts` |
| D79 | **Dated items that add nothing.** The editor warns under an item with no year inside the plan: an ongoing item that ends before it starts, a one-time item before the plan start, or an age already passed. The item is kept, not blocked. | These silently added $0. A warning, not a block, because the item may be kept on purpose (a paid-off loan) and the plan dates can change. | `itemYearsInPlan` in `src/engine/context.ts`, `src/ui/DatedItemsEditor.tsx` |

## Which way the assumptions lean

Which assumptions make results more conservative and which more optimistic, so their combined effect is visible.

| Leans conservative (later date / bigger number) | Leans optimistic | Neutral / depends |
|---|---|---|
| *(core)* 90% target, age 96 of the younger spouse, stricter-of-two, no ACA subsidies, healthcare +1.5%/yr, trust-fund cut; D5 re-check only moves answers later; D7 withdraw first; D14 59½ as age 60; D15 over-limit savings dropped; D24/D77 no real wage growth in Social Security by default; D32 no senior bonus deduction; D39 fill can cost 15% on gains; D40 no losses; D41 HSA penalty to 65 of the younger; D17 a pre-retirement cost savings can't cover fails the path | D25 no earnings test; D13 both spouses alive to the end: two Social Security checks and joint tax brackets throughout (only the spending side, which never drops after a death, leans the other way); D7 contributions also land at the start of the year and earn a full year's return; D15 real wage growth continues until work stops at any age; D32 indexed tax amounts held constant in CPI terms although the IRS indexes them to the slower chained CPI (~0.25%/yr), so brackets and the fill room are slightly too wide on long plans | D42/D51 average-return projection for the FIRE number's account mix; D70 fixed dividend and interest yields; US-only data (history's big winner — optimistic in a global sense) |

## Data update history

| Date | What changed |
|---|---|
| 2026-09-24 | Initial data: market 1871–2025 (Shiller Sep-2026 file, Damodaran through 2025); 2026 IRS brackets/limits; SSA AWI through 2024, 2026 bend points ($1,286/$7,749) and taxable max ($184,500); 2026 Trustees Report. |
| 2026-09-24 | Example healthcare costs raised to $16,000 (before 65) / $7,500 (from 65) per person from 2026 KFF and CMS figures (D58). |
